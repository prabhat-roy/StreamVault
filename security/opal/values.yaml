# OPAL — Open Policy Administration Layer for OPA
image: { repository: permitio/opal-server, tag: latest }
opal:
  policySource: { type: git, url: "https://github.com/streamvault/policies", branch: main }
  dataSources: [{ url: "http://config-service.streamvault.svc/policy-data", topics: ["policy_data"] }]
clients: { autoStart: true }
