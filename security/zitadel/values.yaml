# ZITADEL — developer-platform auth (API keys, OAuth clients, dev portal SSO)
image: { repository: ghcr.io/zitadel/zitadel, tag: latest }
zitadel:
  externalDomain: zitadel.streamvault.internal
  externalSecure: true
  database: { postgres: { host: postgres.streamvault.svc, database: zitadel } }
ingress: { enabled: true, hosts: [{ host: zitadel.streamvault.internal }] }
