apiVersion: v1
kind: Service
metadata:
  name: {{ include "thumbnail-service.fullname" . }}
  labels: {{- include "thumbnail-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "thumbnail-service.selectorLabels" . | nindent 4 }}
