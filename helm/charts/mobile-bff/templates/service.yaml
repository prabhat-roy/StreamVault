apiVersion: v1
kind: Service
metadata:
  name: {{ include "mobile-bff.fullname" . }}
  labels: {{- include "mobile-bff.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "mobile-bff.selectorLabels" . | nindent 4 }}
