apiVersion: v1
kind: Service
metadata:
  name: {{ include "clipping-service.fullname" . }}
  labels: {{- include "clipping-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "clipping-service.selectorLabels" . | nindent 4 }}
