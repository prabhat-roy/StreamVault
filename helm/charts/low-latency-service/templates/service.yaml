apiVersion: v1
kind: Service
metadata:
  name: {{ include "low-latency-service.fullname" . }}
  labels: {{- include "low-latency-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "low-latency-service.selectorLabels" . | nindent 4 }}
