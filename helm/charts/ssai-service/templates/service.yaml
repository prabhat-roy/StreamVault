apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssai-service.fullname" . }}
  labels: {{- include "ssai-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "ssai-service.selectorLabels" . | nindent 4 }}
