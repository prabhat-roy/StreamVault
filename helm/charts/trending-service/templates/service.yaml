apiVersion: v1
kind: Service
metadata:
  name: {{ include "trending-service.fullname" . }}
  labels: {{- include "trending-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "trending-service.selectorLabels" . | nindent 4 }}
