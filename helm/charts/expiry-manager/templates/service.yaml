apiVersion: v1
kind: Service
metadata:
  name: {{ include "expiry-manager.fullname" . }}
  labels: {{- include "expiry-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "expiry-manager.selectorLabels" . | nindent 4 }}
