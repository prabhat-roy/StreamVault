apiVersion: v1
kind: Service
metadata:
  name: {{ include "trial-service.fullname" . }}
  labels: {{- include "trial-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "trial-service.selectorLabels" . | nindent 4 }}
