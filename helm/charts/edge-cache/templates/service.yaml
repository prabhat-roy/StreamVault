apiVersion: v1
kind: Service
metadata:
  name: {{ include "edge-cache.fullname" . }}
  labels: {{- include "edge-cache.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "edge-cache.selectorLabels" . | nindent 4 }}
