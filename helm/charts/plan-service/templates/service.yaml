apiVersion: v1
kind: Service
metadata:
  name: {{ include "plan-service.fullname" . }}
  labels: {{- include "plan-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "plan-service.selectorLabels" . | nindent 4 }}
