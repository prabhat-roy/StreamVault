apiVersion: v1
kind: Service
metadata:
  name: {{ include "origin-service.fullname" . }}
  labels: {{- include "origin-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "origin-service.selectorLabels" . | nindent 4 }}
