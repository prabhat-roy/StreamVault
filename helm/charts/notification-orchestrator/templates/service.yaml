apiVersion: v1
kind: Service
metadata:
  name: {{ include "notification-orchestrator.fullname" . }}
  labels: {{- include "notification-orchestrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "notification-orchestrator.selectorLabels" . | nindent 4 }}
