apiVersion: v1
kind: Service
metadata:
  name: {{ include "push-service.fullname" . }}
  labels: {{- include "push-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "push-service.selectorLabels" . | nindent 4 }}
