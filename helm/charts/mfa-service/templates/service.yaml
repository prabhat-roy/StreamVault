apiVersion: v1
kind: Service
metadata:
  name: {{ include "mfa-service.fullname" . }}
  labels: {{- include "mfa-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "mfa-service.selectorLabels" . | nindent 4 }}
