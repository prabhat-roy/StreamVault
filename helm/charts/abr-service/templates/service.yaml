apiVersion: v1
kind: Service
metadata:
  name: {{ include "abr-service.fullname" . }}
  labels: {{- include "abr-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "abr-service.selectorLabels" . | nindent 4 }}
