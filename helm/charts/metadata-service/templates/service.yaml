apiVersion: v1
kind: Service
metadata:
  name: {{ include "metadata-service.fullname" . }}
  labels: {{- include "metadata-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "metadata-service.selectorLabels" . | nindent 4 }}
