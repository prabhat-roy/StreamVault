apiVersion: v1
kind: Service
metadata:
  name: {{ include "dai-service.fullname" . }}
  labels: {{- include "dai-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "dai-service.selectorLabels" . | nindent 4 }}
