apiVersion: v1
kind: Service
metadata:
  name: {{ include "parental-controls.fullname" . }}
  labels: {{- include "parental-controls.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "parental-controls.selectorLabels" . | nindent 4 }}
