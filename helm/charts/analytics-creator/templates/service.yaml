apiVersion: v1
kind: Service
metadata:
  name: {{ include "analytics-creator.fullname" . }}
  labels: {{- include "analytics-creator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "analytics-creator.selectorLabels" . | nindent 4 }}
