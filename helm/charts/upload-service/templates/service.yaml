apiVersion: v1
kind: Service
metadata:
  name: {{ include "upload-service.fullname" . }}
  labels: {{- include "upload-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "upload-service.selectorLabels" . | nindent 4 }}
