apiVersion: v1
kind: Service
metadata:
  name: {{ include "ad-server.fullname" . }}
  labels: {{- include "ad-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "ad-server.selectorLabels" . | nindent 4 }}
