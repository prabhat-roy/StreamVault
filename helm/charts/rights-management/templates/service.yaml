apiVersion: v1
kind: Service
metadata:
  name: {{ include "rights-management.fullname" . }}
  labels: {{- include "rights-management.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "rights-management.selectorLabels" . | nindent 4 }}
