apiVersion: v1
kind: Service
metadata:
  name: {{ include "revenue-analytics.fullname" . }}
  labels: {{- include "revenue-analytics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "revenue-analytics.selectorLabels" . | nindent 4 }}
