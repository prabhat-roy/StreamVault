apiVersion: v1
kind: Service
metadata:
  name: {{ include "audio-service.fullname" . }}
  labels: {{- include "audio-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "audio-service.selectorLabels" . | nindent 4 }}
