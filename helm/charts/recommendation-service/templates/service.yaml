apiVersion: v1
kind: Service
metadata:
  name: {{ include "recommendation-service.fullname" . }}
  labels: {{- include "recommendation-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "recommendation-service.selectorLabels" . | nindent 4 }}
