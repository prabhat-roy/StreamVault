apiVersion: v1
kind: Service
metadata:
  name: {{ include "watch-party-service.fullname" . }}
  labels: {{- include "watch-party-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "watch-party-service.selectorLabels" . | nindent 4 }}
