apiVersion: v1
kind: Service
metadata:
  name: {{ include "collection-service.fullname" . }}
  labels: {{- include "collection-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "collection-service.selectorLabels" . | nindent 4 }}
