apiVersion: v1
kind: Service
metadata:
  name: {{ include "studio-dashboard.fullname" . }}
  labels: {{- include "studio-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "studio-dashboard.selectorLabels" . | nindent 4 }}
