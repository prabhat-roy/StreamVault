apiVersion: v1
kind: Service
metadata:
  name: {{ include "session-service.fullname" . }}
  labels: {{- include "session-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "session-service.selectorLabels" . | nindent 4 }}
