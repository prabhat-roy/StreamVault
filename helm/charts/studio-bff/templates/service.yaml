apiVersion: v1
kind: Service
metadata:
  name: {{ include "studio-bff.fullname" . }}
  labels: {{- include "studio-bff.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "studio-bff.selectorLabels" . | nindent 4 }}
