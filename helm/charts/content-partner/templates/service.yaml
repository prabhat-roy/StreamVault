apiVersion: v1
kind: Service
metadata:
  name: {{ include "content-partner.fullname" . }}
  labels: {{- include "content-partner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "content-partner.selectorLabels" . | nindent 4 }}
