apiVersion: v1
kind: Service
metadata:
  name: {{ include "playback-analytics.fullname" . }}
  labels: {{- include "playback-analytics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "playback-analytics.selectorLabels" . | nindent 4 }}
