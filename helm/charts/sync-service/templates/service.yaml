apiVersion: v1
kind: Service
metadata:
  name: {{ include "sync-service.fullname" . }}
  labels: {{- include "sync-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "sync-service.selectorLabels" . | nindent 4 }}
