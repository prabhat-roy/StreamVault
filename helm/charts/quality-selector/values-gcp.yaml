cloud:
  provider: gcp
  storageClass: pd-balanced
  ingressClass: gce

serviceAccount:
  annotations:
    iam.gke.io/gcp-service-account: streamvault-quality-selector@{{ .Values.gcp.project }}.iam.gserviceaccount.com
