apiVersion: v1
kind: Service
metadata:
  name: {{ include "targeting-service.fullname" . }}
  labels: {{- include "targeting-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "targeting-service.selectorLabels" . | nindent 4 }}
