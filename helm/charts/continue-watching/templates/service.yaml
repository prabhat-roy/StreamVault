apiVersion: v1
kind: Service
metadata:
  name: {{ include "continue-watching.fullname" . }}
  labels: {{- include "continue-watching.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "continue-watching.selectorLabels" . | nindent 4 }}
