apiVersion: v1
kind: Service
metadata:
  name: {{ include "ad-analytics.fullname" . }}
  labels: {{- include "ad-analytics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "ad-analytics.selectorLabels" . | nindent 4 }}
