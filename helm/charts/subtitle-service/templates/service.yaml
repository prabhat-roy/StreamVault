apiVersion: v1
kind: Service
metadata:
  name: {{ include "subtitle-service.fullname" . }}
  labels: {{- include "subtitle-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "subtitle-service.selectorLabels" . | nindent 4 }}
