apiVersion: v1
kind: Service
metadata:
  name: {{ include "ad-insertion.fullname" . }}
  labels: {{- include "ad-insertion.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "ad-insertion.selectorLabels" . | nindent 4 }}
