apiVersion: v1
kind: Service
metadata:
  name: {{ include "geo-routing.fullname" . }}
  labels: {{- include "geo-routing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "geo-routing.selectorLabels" . | nindent 4 }}
