apiVersion: v1
kind: Service
metadata:
  name: {{ include "search-ranking.fullname" . }}
  labels: {{- include "search-ranking.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "search-ranking.selectorLabels" . | nindent 4 }}
