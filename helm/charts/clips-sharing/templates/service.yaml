apiVersion: v1
kind: Service
metadata:
  name: {{ include "clips-sharing.fullname" . }}
  labels: {{- include "clips-sharing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "clips-sharing.selectorLabels" . | nindent 4 }}
