opencost:
  prometheus: { internal: { enabled: false }, external: { url: http://prometheus.observability:9090 } }
  ui: { enabled: true }
  exporter:
    cloudProviderApiKey: ""
    defaultClusterId: streamvault-prod
ingress:
  enabled: true
  hosts: [{ host: opencost.streamvault.internal, paths: [{ path: /, pathType: Prefix }] }]
