name: streamvault
description: StreamVault — Pulumi alternative IaC (mirrors infra/terraform/{aws,gcp,azure})
runtime: { name: nodejs, options: { typescript: true } }
backend:
  url: s3://streamvault-pulumi-state
config:
  streamvault:env: prod
  streamvault:cloud: aws
