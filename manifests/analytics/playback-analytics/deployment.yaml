apiVersion: apps/v1
kind: Deployment
metadata:
  name: playback-analytics
  namespace: streamvault
  labels:
    app.kubernetes.io/name: playback-analytics
    app.kubernetes.io/part-of: streamvault
    streamvault.io/domain: analytics
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: playback-analytics } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: playback-analytics, streamvault.io/domain: analytics }
    spec:
      serviceAccountName: playback-analytics
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: playback-analytics
          image: harbor.streamvault.internal/streamvault/playback-analytics:latest
          ports: [{ containerPort: 50301, name: http }]
          envFrom: [{ configMapRef: { name: playback-analytics } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50301 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50301 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
