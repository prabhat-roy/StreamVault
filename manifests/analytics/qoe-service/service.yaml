apiVersion: v1
kind: Service
metadata:
  name: qoe-service
  namespace: streamvault
  labels: { app.kubernetes.io/name: qoe-service, streamvault.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50300, targetPort: 50300 }]
  selector: { app.kubernetes.io/name: qoe-service }
