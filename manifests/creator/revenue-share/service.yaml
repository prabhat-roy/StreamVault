apiVersion: v1
kind: Service
metadata:
  name: revenue-share
  namespace: streamvault
  labels: { app.kubernetes.io/name: revenue-share, streamvault.io/domain: creator }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50203, targetPort: 50203 }]
  selector: { app.kubernetes.io/name: revenue-share }
