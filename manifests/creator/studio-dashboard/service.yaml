apiVersion: v1
kind: Service
metadata:
  name: studio-dashboard
  namespace: streamvault
  labels: { app.kubernetes.io/name: studio-dashboard, streamvault.io/domain: creator }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50201, targetPort: 50201 }]
  selector: { app.kubernetes.io/name: studio-dashboard }
