apiVersion: v1
kind: Service
metadata:
  name: analytics-creator
  namespace: streamvault
  labels: { app.kubernetes.io/name: analytics-creator, streamvault.io/domain: creator }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50202, targetPort: 50202 }]
  selector: { app.kubernetes.io/name: analytics-creator }
