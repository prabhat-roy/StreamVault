apiVersion: v1
kind: Service
metadata:
  name: upload-service
  namespace: streamvault
  labels: { app.kubernetes.io/name: upload-service, streamvault.io/domain: creator }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50200, targetPort: 50200 }]
  selector: { app.kubernetes.io/name: upload-service }
