apiVersion: apps/v1
kind: Deployment
metadata:
  name: watermarking-service
  namespace: streamvault
  labels:
    app.kubernetes.io/name: watermarking-service
    app.kubernetes.io/part-of: streamvault
    streamvault.io/domain: drm
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: watermarking-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: watermarking-service, streamvault.io/domain: drm }
    spec:
      serviceAccountName: watermarking-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: watermarking-service
          image: harbor.streamvault.internal/streamvault/watermarking-service:latest
          ports: [{ containerPort: 50081, name: http }]
          envFrom: [{ configMapRef: { name: watermarking-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50081 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50081 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
