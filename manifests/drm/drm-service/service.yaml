apiVersion: v1
kind: Service
metadata:
  name: drm-service
  namespace: streamvault
  labels: { app.kubernetes.io/name: drm-service, streamvault.io/domain: drm }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50030, targetPort: 50030 }]
  selector: { app.kubernetes.io/name: drm-service }
