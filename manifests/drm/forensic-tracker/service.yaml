apiVersion: v1
kind: Service
metadata:
  name: forensic-tracker
  namespace: streamvault
  labels: { app.kubernetes.io/name: forensic-tracker, streamvault.io/domain: drm }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50082, targetPort: 50082 }]
  selector: { app.kubernetes.io/name: forensic-tracker }
