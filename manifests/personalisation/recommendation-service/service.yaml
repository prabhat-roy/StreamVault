apiVersion: v1
kind: Service
metadata:
  name: recommendation-service
  namespace: streamvault
  labels: { app.kubernetes.io/name: recommendation-service, streamvault.io/domain: personalisation }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50070, targetPort: 50070 }]
  selector: { app.kubernetes.io/name: recommendation-service }
