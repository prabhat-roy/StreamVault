apiVersion: v1
kind: ConfigMap
metadata: { name: recommendation-service, namespace: streamvault }
data:
  LOG_LEVEL: info
  PORT: "50070"
  MODEL_PATH: /models/two-tower
  VECTOR_DB_URL: http://weaviate.databases:8080
