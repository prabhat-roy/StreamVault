apiVersion: apps/v1
kind: Deployment
metadata:
  name: trending-service
  namespace: streamvault
  labels:
    app.kubernetes.io/name: trending-service
    app.kubernetes.io/part-of: streamvault
    streamvault.io/domain: personalisation
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: trending-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: trending-service, streamvault.io/domain: personalisation }
    spec:
      serviceAccountName: trending-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: trending-service
          image: harbor.streamvault.internal/streamvault/trending-service:latest
          ports: [{ containerPort: 50242, name: http }]
          envFrom: [{ configMapRef: { name: trending-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50242 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50242 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
