apiVersion: apps/v1
kind: Deployment
metadata:
  name: targeting-service
  namespace: streamvault
  labels:
    app.kubernetes.io/name: targeting-service
    app.kubernetes.io/part-of: streamvault
    streamvault.io/domain: ad
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: targeting-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: targeting-service, streamvault.io/domain: ad }
    spec:
      serviceAccountName: targeting-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: targeting-service
          image: harbor.streamvault.internal/streamvault/targeting-service:latest
          ports: [{ containerPort: 50222, name: http }]
          envFrom: [{ configMapRef: { name: targeting-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50222 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50222 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
