apiVersion: v1
kind: Service
metadata:
  name: ssai-service
  namespace: streamvault
  labels: { app.kubernetes.io/name: ssai-service, streamvault.io/domain: ad }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50224, targetPort: 50224 }]
  selector: { app.kubernetes.io/name: ssai-service }
