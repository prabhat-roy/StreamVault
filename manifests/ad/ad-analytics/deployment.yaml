apiVersion: apps/v1
kind: Deployment
metadata:
  name: ad-analytics
  namespace: streamvault
  labels:
    app.kubernetes.io/name: ad-analytics
    app.kubernetes.io/part-of: streamvault
    streamvault.io/domain: ad
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: ad-analytics } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: ad-analytics, streamvault.io/domain: ad }
    spec:
      serviceAccountName: ad-analytics
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: ad-analytics
          image: harbor.streamvault.internal/streamvault/ad-analytics:latest
          ports: [{ containerPort: 50223, name: http }]
          envFrom: [{ configMapRef: { name: ad-analytics } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50223 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50223 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
