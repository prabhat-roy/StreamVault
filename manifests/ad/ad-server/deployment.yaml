apiVersion: apps/v1
kind: Deployment
metadata:
  name: ad-server
  namespace: streamvault
  labels:
    app.kubernetes.io/name: ad-server
    app.kubernetes.io/part-of: streamvault
    streamvault.io/domain: ad
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: ad-server } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: ad-server, streamvault.io/domain: ad }
    spec:
      serviceAccountName: ad-server
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: ad-server
          image: harbor.streamvault.internal/streamvault/ad-server:latest
          ports: [{ containerPort: 50220, name: http }]
          envFrom: [{ configMapRef: { name: ad-server } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50220 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50220 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
