apiVersion: v1
kind: Service
metadata:
  name: dai-service
  namespace: streamvault
  labels: { app.kubernetes.io/name: dai-service, streamvault.io/domain: ad }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50221, targetPort: 50221 }]
  selector: { app.kubernetes.io/name: dai-service }
