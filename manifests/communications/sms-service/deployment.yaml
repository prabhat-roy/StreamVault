apiVersion: apps/v1
kind: Deployment
metadata:
  name: sms-service
  namespace: streamvault
  labels:
    app.kubernetes.io/name: sms-service
    app.kubernetes.io/part-of: streamvault
    streamvault.io/domain: communications
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: sms-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: sms-service, streamvault.io/domain: communications }
    spec:
      serviceAccountName: sms-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: sms-service
          image: harbor.streamvault.internal/streamvault/sms-service:latest
          ports: [{ containerPort: 50323, name: http }]
          envFrom: [{ configMapRef: { name: sms-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50323 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50323 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
