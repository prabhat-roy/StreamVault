apiVersion: v1
kind: ConfigMap
metadata:
  name: sms-service
  namespace: streamvault
  labels: { app.kubernetes.io/name: sms-service, streamvault.io/domain: communications }
data:
  LOG_LEVEL: info
  PORT: "50323"
