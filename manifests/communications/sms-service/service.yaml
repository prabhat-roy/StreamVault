apiVersion: v1
kind: Service
metadata:
  name: sms-service
  namespace: streamvault
  labels: { app.kubernetes.io/name: sms-service, streamvault.io/domain: communications }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50323, targetPort: 50323 }]
  selector: { app.kubernetes.io/name: sms-service }
