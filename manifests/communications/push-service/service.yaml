apiVersion: v1
kind: Service
metadata:
  name: push-service
  namespace: streamvault
  labels: { app.kubernetes.io/name: push-service, streamvault.io/domain: communications }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50322, targetPort: 50322 }]
  selector: { app.kubernetes.io/name: push-service }
