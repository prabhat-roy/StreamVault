apiVersion: v1
kind: Service
metadata:
  name: clips-sharing
  namespace: streamvault
  labels: { app.kubernetes.io/name: clips-sharing, streamvault.io/domain: social }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50264, targetPort: 50264 }]
  selector: { app.kubernetes.io/name: clips-sharing }
