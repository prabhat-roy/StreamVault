apiVersion: apps/v1
kind: Deployment
metadata:
  name: rating-service
  namespace: streamvault
  labels:
    app.kubernetes.io/name: rating-service
    app.kubernetes.io/part-of: streamvault
    streamvault.io/domain: social
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: rating-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: rating-service, streamvault.io/domain: social }
    spec:
      serviceAccountName: rating-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: rating-service
          image: harbor.streamvault.internal/streamvault/rating-service:latest
          ports: [{ containerPort: 50261, name: http }]
          envFrom: [{ configMapRef: { name: rating-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50261 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50261 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
