apiVersion: apps/v1
kind: Deployment
metadata:
  name: watchlist-service
  namespace: streamvault
  labels:
    app.kubernetes.io/name: watchlist-service
    app.kubernetes.io/part-of: streamvault
    streamvault.io/domain: social
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: watchlist-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: watchlist-service, streamvault.io/domain: social }
    spec:
      serviceAccountName: watchlist-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: watchlist-service
          image: harbor.streamvault.internal/streamvault/watchlist-service:latest
          ports: [{ containerPort: 50260, name: http }]
          envFrom: [{ configMapRef: { name: watchlist-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50260 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50260 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
