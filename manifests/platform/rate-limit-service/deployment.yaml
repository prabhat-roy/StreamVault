apiVersion: apps/v1
kind: Deployment
metadata:
  name: rate-limit-service
  namespace: streamvault
  labels:
    app.kubernetes.io/name: rate-limit-service
    app.kubernetes.io/part-of: streamvault
    streamvault.io/domain: platform
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: rate-limit-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: rate-limit-service, streamvault.io/domain: platform }
    spec:
      serviceAccountName: rate-limit-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: rate-limit-service
          image: harbor.streamvault.internal/streamvault/rate-limit-service:latest
          ports: [{ containerPort: 51007, name: http }]
          envFrom: [{ configMapRef: { name: rate-limit-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 51007 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 51007 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
