apiVersion: v1
kind: ConfigMap
metadata:
  name: geo-address-service
  namespace: streamvault
  labels: { app.kubernetes.io/name: geo-address-service, streamvault.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51013"
