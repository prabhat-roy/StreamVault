apiVersion: v1
kind: ConfigMap
metadata:
  name: tenant-service
  namespace: streamvault
  labels: { app.kubernetes.io/name: tenant-service, streamvault.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51006"
