apiVersion: v1
kind: ConfigMap
metadata:
  name: file-upload-service
  namespace: streamvault
  labels: { app.kubernetes.io/name: file-upload-service, streamvault.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51011"
