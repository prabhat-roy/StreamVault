apiVersion: v1
kind: ConfigMap
metadata:
  name: webhook-delivery-service
  namespace: streamvault
  labels: { app.kubernetes.io/name: webhook-delivery-service, streamvault.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51004"
