apiVersion: v1
kind: ConfigMap
metadata:
  name: api-key-service
  namespace: streamvault
  labels: { app.kubernetes.io/name: api-key-service, streamvault.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51003"
