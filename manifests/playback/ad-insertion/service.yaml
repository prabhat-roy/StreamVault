apiVersion: v1
kind: Service
metadata:
  name: ad-insertion
  namespace: streamvault
  labels: { app.kubernetes.io/name: ad-insertion, streamvault.io/domain: playback }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50163, targetPort: 50163 }]
  selector: { app.kubernetes.io/name: ad-insertion }
