apiVersion: v1
kind: Service
metadata:
  name: quality-selector
  namespace: streamvault
  labels: { app.kubernetes.io/name: quality-selector, streamvault.io/domain: playback }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50162, targetPort: 50162 }]
  selector: { app.kubernetes.io/name: quality-selector }
