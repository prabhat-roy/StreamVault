apiVersion: v1
kind: Service
metadata:
  name: playback-orchestrator
  namespace: streamvault
  labels: { app.kubernetes.io/name: playback-orchestrator, streamvault.io/domain: playback }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50060, targetPort: 50060 }]
  selector: { app.kubernetes.io/name: playback-orchestrator }
