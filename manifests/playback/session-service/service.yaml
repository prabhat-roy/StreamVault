apiVersion: v1
kind: Service
metadata:
  name: session-service
  namespace: streamvault
  labels: { app.kubernetes.io/name: session-service, streamvault.io/domain: playback }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50160, targetPort: 50160 }]
  selector: { app.kubernetes.io/name: session-service }
