apiVersion: apps/v1
kind: Deployment
metadata:
  name: session-service
  namespace: streamvault
  labels:
    app.kubernetes.io/name: session-service
    app.kubernetes.io/part-of: streamvault
    streamvault.io/domain: playback
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: session-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: session-service, streamvault.io/domain: playback }
    spec:
      serviceAccountName: session-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: session-service
          image: harbor.streamvault.internal/streamvault/session-service:latest
          ports: [{ containerPort: 50160, name: http }]
          envFrom: [{ configMapRef: { name: session-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50160 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50160 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
