apiVersion: v1
kind: Service
metadata:
  name: stream-packager
  namespace: streamvault
  labels: { app.kubernetes.io/name: stream-packager, streamvault.io/domain: live }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50040, targetPort: 50040 }]
  selector: { app.kubernetes.io/name: stream-packager }
