apiVersion: v1
kind: Service
metadata:
  name: dvr-service
  namespace: streamvault
  labels: { app.kubernetes.io/name: dvr-service, streamvault.io/domain: live }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50122, targetPort: 50122 }]
  selector: { app.kubernetes.io/name: dvr-service }
