apiVersion: v1
kind: Service
metadata:
  name: low-latency-service
  namespace: streamvault
  labels: { app.kubernetes.io/name: low-latency-service, streamvault.io/domain: live }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50123, targetPort: 50123 }]
  selector: { app.kubernetes.io/name: low-latency-service }
