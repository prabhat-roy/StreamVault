apiVersion: v1
kind: Service
metadata:
  name: live-ingest
  namespace: streamvault
  labels: { app.kubernetes.io/name: live-ingest, streamvault.io/domain: live }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50120, targetPort: 50120 }]
  selector: { app.kubernetes.io/name: live-ingest }
