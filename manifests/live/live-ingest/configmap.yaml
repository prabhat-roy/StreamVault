apiVersion: v1
kind: ConfigMap
metadata:
  name: live-ingest
  namespace: streamvault
  labels: { app.kubernetes.io/name: live-ingest, streamvault.io/domain: live }
data:
  LOG_LEVEL: info
  PORT: "50120"
