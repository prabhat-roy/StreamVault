apiVersion: v1
kind: ConfigMap
metadata:
  name: clipping-service
  namespace: streamvault
  labels: { app.kubernetes.io/name: clipping-service, streamvault.io/domain: live }
data:
  LOG_LEVEL: info
  PORT: "50124"
