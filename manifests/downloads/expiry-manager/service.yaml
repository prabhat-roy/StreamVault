apiVersion: v1
kind: Service
metadata:
  name: expiry-manager
  namespace: streamvault
  labels: { app.kubernetes.io/name: expiry-manager, streamvault.io/domain: downloads }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50283, targetPort: 50283 }]
  selector: { app.kubernetes.io/name: expiry-manager }
