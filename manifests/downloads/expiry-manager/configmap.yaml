apiVersion: v1
kind: ConfigMap
metadata:
  name: expiry-manager
  namespace: streamvault
  labels: { app.kubernetes.io/name: expiry-manager, streamvault.io/domain: downloads }
data:
  LOG_LEVEL: info
  PORT: "50283"
