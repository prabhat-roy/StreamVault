apiVersion: apps/v1
kind: Deployment
metadata:
  name: download-service
  namespace: streamvault
  labels:
    app.kubernetes.io/name: download-service
    app.kubernetes.io/part-of: streamvault
    streamvault.io/domain: downloads
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: download-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: download-service, streamvault.io/domain: downloads }
    spec:
      serviceAccountName: download-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: download-service
          image: harbor.streamvault.internal/streamvault/download-service:latest
          ports: [{ containerPort: 50280, name: http }]
          envFrom: [{ configMapRef: { name: download-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50280 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50280 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
