apiVersion: v1
kind: Service
metadata:
  name: download-service
  namespace: streamvault
  labels: { app.kubernetes.io/name: download-service, streamvault.io/domain: downloads }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50280, targetPort: 50280 }]
  selector: { app.kubernetes.io/name: download-service }
