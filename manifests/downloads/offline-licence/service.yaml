apiVersion: v1
kind: Service
metadata:
  name: offline-licence
  namespace: streamvault
  labels: { app.kubernetes.io/name: offline-licence, streamvault.io/domain: downloads }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50281, targetPort: 50281 }]
  selector: { app.kubernetes.io/name: offline-licence }
