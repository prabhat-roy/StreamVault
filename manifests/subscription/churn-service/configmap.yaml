apiVersion: v1
kind: ConfigMap
metadata:
  name: churn-service
  namespace: streamvault
  labels: { app.kubernetes.io/name: churn-service, streamvault.io/domain: subscription }
data:
  LOG_LEVEL: info
  PORT: "50183"
