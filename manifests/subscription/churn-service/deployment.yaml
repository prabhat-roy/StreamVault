apiVersion: apps/v1
kind: Deployment
metadata:
  name: churn-service
  namespace: streamvault
  labels:
    app.kubernetes.io/name: churn-service
    app.kubernetes.io/part-of: streamvault
    streamvault.io/domain: subscription
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: churn-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: churn-service, streamvault.io/domain: subscription }
    spec:
      serviceAccountName: churn-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: churn-service
          image: harbor.streamvault.internal/streamvault/churn-service:latest
          ports: [{ containerPort: 50183, name: http }]
          envFrom: [{ configMapRef: { name: churn-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50183 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50183 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
