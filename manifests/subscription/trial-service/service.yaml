apiVersion: v1
kind: Service
metadata:
  name: trial-service
  namespace: streamvault
  labels: { app.kubernetes.io/name: trial-service, streamvault.io/domain: subscription }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50182, targetPort: 50182 }]
  selector: { app.kubernetes.io/name: trial-service }
