apiVersion: v1
kind: Service
metadata:
  name: plan-service
  namespace: streamvault
  labels: { app.kubernetes.io/name: plan-service, streamvault.io/domain: subscription }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50180, targetPort: 50180 }]
  selector: { app.kubernetes.io/name: plan-service }
