apiVersion: apps/v1
kind: Deployment
metadata:
  name: collection-service
  namespace: streamvault
  labels:
    app.kubernetes.io/name: collection-service
    app.kubernetes.io/part-of: streamvault
    streamvault.io/domain: catalogue
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: collection-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: collection-service, streamvault.io/domain: catalogue }
    spec:
      serviceAccountName: collection-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: collection-service
          image: harbor.streamvault.internal/streamvault/collection-service:latest
          ports: [{ containerPort: 50141, name: http }]
          envFrom: [{ configMapRef: { name: collection-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50141 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50141 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
