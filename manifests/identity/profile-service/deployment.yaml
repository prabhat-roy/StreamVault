apiVersion: apps/v1
kind: Deployment
metadata:
  name: profile-service
  namespace: streamvault
  labels:
    app.kubernetes.io/name: profile-service
    app.kubernetes.io/part-of: streamvault
    streamvault.io/domain: identity
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: profile-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: profile-service, streamvault.io/domain: identity }
    spec:
      serviceAccountName: profile-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: profile-service
          image: harbor.streamvault.internal/streamvault/profile-service:latest
          ports: [{ containerPort: 50021, name: http }]
          envFrom: [{ configMapRef: { name: profile-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50021 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50021 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
