apiVersion: v1
kind: Service
metadata:
  name: edge-cache
  namespace: streamvault
  labels: { app.kubernetes.io/name: edge-cache, streamvault.io/domain: cdn }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50102, targetPort: 50102 }]
  selector: { app.kubernetes.io/name: edge-cache }
