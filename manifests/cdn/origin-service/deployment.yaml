apiVersion: apps/v1
kind: Deployment
metadata:
  name: origin-service
  namespace: streamvault
  labels:
    app.kubernetes.io/name: origin-service
    app.kubernetes.io/part-of: streamvault
    streamvault.io/domain: cdn
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: origin-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: origin-service, streamvault.io/domain: cdn }
    spec:
      serviceAccountName: origin-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: origin-service
          image: harbor.streamvault.internal/streamvault/origin-service:latest
          ports: [{ containerPort: 50101, name: http }]
          envFrom: [{ configMapRef: { name: origin-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50101 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50101 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
