apiVersion: apps/v1
kind: Deployment
metadata:
  name: abr-service
  namespace: streamvault
  labels:
    app.kubernetes.io/name: abr-service
    app.kubernetes.io/part-of: streamvault
    streamvault.io/domain: cdn
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: abr-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: abr-service, streamvault.io/domain: cdn }
    spec:
      serviceAccountName: abr-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: abr-service
          image: harbor.streamvault.internal/streamvault/abr-service:latest
          ports: [{ containerPort: 50104, name: http }]
          envFrom: [{ configMapRef: { name: abr-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50104 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50104 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
