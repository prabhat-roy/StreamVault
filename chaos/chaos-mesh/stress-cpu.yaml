apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata: { name: streamvault-cpu-stress, namespace: chaos-mesh }
spec:
  mode: one
  selector: { namespaces: [streamvault] }
  stressors: { cpu: { workers: 4, load: 80 } }
  duration: 5m
