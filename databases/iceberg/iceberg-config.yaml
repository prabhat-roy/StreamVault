catalog:
  name: streamvault_lake
  type: rest
  uri: https://nessie.streamvault.internal/iceberg
warehouse: s3://streamvault-data-lake/warehouse
io:
  s3:
    endpoint: https://s3.amazonaws.com
    region: us-east-1
table-default:
  write.format.default: parquet
  write.metadata.compression-codec: gzip
