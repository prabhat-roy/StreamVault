# in-toto layout — supply-chain step attestations for StreamVault
expires: "2027-01-01T00:00:00Z"
keys: { project-key: { keyid: streamvault-key, keytype: ed25519, scheme: ed25519 } }
steps:
  - { name: source, expected_command: ["git", "clone"], pubkeys: [streamvault-key] }
  - { name: build, expected_command: ["docker", "build"], pubkeys: [streamvault-key] }
  - { name: test, expected_command: ["make", "test"], pubkeys: [streamvault-key] }
inspect:
  - { name: signed-image, run: ["cosign", "verify"], expected_materials: [], expected_products: [] }
