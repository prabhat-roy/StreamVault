apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata: { name: streamvault, namespace: flux-system }
spec:
  interval: 1m
  url: https://github.com/prabhat-roy/StreamVault.git
  ref: { branch: main }
