apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: streamvault-tools
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/prabhat-roy/StreamVault.git
        revision: HEAD
        directories:
          - { path: security/* }
          - { path: observability/* }
          - { path: messaging/* }
          - { path: networking/* }
          - { path: databases/* }
          - { path: data/* }
          - { path: supply-chain/* }
          - { path: sustainability/* }
          - { path: edge/* }
          - { path: streaming/* }
          - { path: workflow/* }
          - { path: registry/charts/* }
          - { path: domain-oss/* }
  template:
    metadata:
      name: "streamvault-tool-{{path.basename}}"
      labels: { project: streamvault-tools, area: "{{path[0]}}" }
    spec:
      project: default
      source:
        repoURL: https://github.com/prabhat-roy/StreamVault.git
        targetRevision: HEAD
        path: "{{path}}"
        helm:
          releaseName: "{{path.basename}}"
          valueFiles: ["values.yaml"]
      destination:
        server: https://kubernetes.default.svc
        namespace: "streamvault-tools"
      syncPolicy:
        automated: { prune: true, selfHeal: true }
        syncOptions: [CreateNamespace=true, ServerSideApply=true]
